---
id: component:schema
title: schema
state: specified
confidence: verified
owner: vfeenstr
level: component
responsibility: Emits the JSON Schema for the file formats so editors
  autocomplete and inline-flag while an element is authored. The repo commits
  the output and --check fails when it has drifted.
parent: component:ab
implemented_by:
- absicht#src/absicht/schema.py
---
