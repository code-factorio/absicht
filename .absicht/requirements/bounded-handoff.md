---
id: requirement:bounded-handoff
title: Hand an agent a bounded packet
state: specified
confidence: reviewed
owner: vfeenstr
realized_by:
- component:packet
- component:gherkin
- component:runstore
---

Milestone scope at full fidelity, one ring of neighbouring contracts, the
decisions and NFRs that must hold, explicit freedoms, known unknowns, the
rejections that must not be re-proposed — and two behavior lists: the
behaviors this slice must satisfy and the active behaviors it must not
break. Sealed with design rev plus scenarios digest so verify runs offline.
Issuance is recorded in the local run store.
