{
  "$defs": {
    "Confidence": {
      "description": "How much we trust this, independent of how complete it is.",
      "enum": [
        "assumed",
        "reviewed",
        "verified"
      ],
      "title": "Confidence",
      "type": "string"
    },
    "Lifecycle": {
      "description": "Whether an element is still how the system works.\n\nA second axis to `state`, which says how well specified it is: a\nbehavior can be perfectly `specified` and no longer true.",
      "enum": [
        "active",
        "superseded"
      ],
      "title": "Lifecycle",
      "type": "string"
    },
    "Observation": {
      "additionalProperties": false,
      "description": "One expectation about how the system acts, anchored to its behavior.\n\nThe counterpart of `Criterion`: a criterion says when a story is done,\nan observation says how you would know the system acted. `at` names what\nit points at — a component, a resource, a seam, or another behavior\n(composition); which of those are legitimate targets is `check`'s to\nsay, not this file's.",
      "properties": {
        "id": {
          "pattern": "^[a-z]+:[a-z0-9][a-z0-9-]*#obs-\\d+$",
          "title": "Id",
          "type": "string"
        },
        "statement": {
          "minLength": 1,
          "title": "Statement",
          "type": "string"
        },
        "at": {
          "pattern": "^[a-z]+:[a-z0-9][a-z0-9-]*$",
          "title": "At",
          "type": "string"
        },
        "outcome": {
          "$ref": "#/$defs/Outcome",
          "default": "must"
        },
        "timing": {
          "anyOf": [
            {
              "$ref": "#/$defs/Timing"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional on purpose: the effective timing is computed, never defaulted\nhere — an authored value wins, else the default follows what `at` points\nat (`effective_timing`). Storing a default would make \"not said\" and\n\"deliberately immediate\" indistinguishable."
        }
      },
      "required": [
        "id",
        "statement",
        "at"
      ],
      "title": "Observation",
      "type": "object"
    },
    "Outcome": {
      "description": "Whether an observation is required, forbidden, or advisory.",
      "enum": [
        "must",
        "must_not",
        "should"
      ],
      "title": "Outcome",
      "type": "string"
    },
    "State": {
      "description": "How complete an element is, and what an agent may do about it.",
      "enum": [
        "specified",
        "constrained",
        "delegated",
        "unknown",
        "observed",
        "out_of_scope"
      ],
      "title": "State",
      "type": "string"
    },
    "Timing": {
      "description": "When an observation's outcome becomes true.",
      "enum": [
        "immediate",
        "eventual"
      ],
      "title": "Timing",
      "type": "string"
    }
  },
  "additionalProperties": false,
  "description": "An expectation about how the system acts.\n\nThe counterpart to a requirement: a requirement is what and why, a\nbehavior is how you would know. Behaviors are permanent — not owned by\na milestone, not expiring when one completes (§5). Supersession is\nrecorded on the replacement (`supersedes`) and never mirrored as a\nstored `superseded_by`: same inversion as `parent` with no `children[]`.\n\nAn empty `observations` tuple is valid here. A behavior mid-authoring is\nlegitimate on disk; whether a finished one needs observations is\n`policy/behavior-needs-observations`'s report line, not an exception.",
  "properties": {
    "id": {
      "pattern": "^[a-z]+:[a-z0-9][a-z0-9-]*$",
      "title": "Id",
      "type": "string"
    },
    "title": {
      "minLength": 1,
      "title": "Title",
      "type": "string"
    },
    "state": {
      "$ref": "#/$defs/State",
      "default": "unknown"
    },
    "confidence": {
      "$ref": "#/$defs/Confidence",
      "default": "assumed"
    },
    "owner": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Owner"
    },
    "tags": {
      "default": [],
      "items": {
        "type": "string"
      },
      "title": "Tags",
      "type": "array"
    },
    "source": {
      "default": "",
      "description": "Path within the store. Written by the loader.",
      "title": "Source",
      "type": "string"
    },
    "body": {
      "default": "",
      "description": "Prose, verbatim. Never parsed.",
      "title": "Body",
      "type": "string"
    },
    "trigger": {
      "description": "Prose — a sentence naming what happened. Not a condition, not Gherkin.",
      "minLength": 1,
      "title": "Trigger",
      "type": "string"
    },
    "realizes": {
      "default": [],
      "items": {
        "pattern": "^[a-z]+:[a-z0-9][a-z0-9-]*$",
        "type": "string"
      },
      "title": "Realizes",
      "type": "array"
    },
    "lifecycle": {
      "$ref": "#/$defs/Lifecycle",
      "default": "active"
    },
    "supersedes": {
      "default": [],
      "items": {
        "pattern": "^[a-z]+:[a-z0-9][a-z0-9-]*$",
        "type": "string"
      },
      "title": "Supersedes",
      "type": "array"
    },
    "observations": {
      "default": [],
      "items": {
        "$ref": "#/$defs/Observation"
      },
      "title": "Observations",
      "type": "array"
    }
  },
  "required": [
    "id",
    "title",
    "trigger"
  ],
  "title": "Behavior",
  "type": "object"
}
