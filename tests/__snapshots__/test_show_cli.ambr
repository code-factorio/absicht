# serializer version: 1
# name: test_the_text_format_is_snapshotted
  '''
  component:orders — Orders
    state: specified
    confidence: reviewed
    lifecycle: active
    owner: dana
    level: container
    responsibility: Take orders and record what happened to them.
    technology: Python 3.14
    parent: component:acme
    implemented_by: acme#src/orders
  
  points at:
    component:acme (parent)
    req:cancel-orders (implements)
    constraint:gdpr-erasure (constrained_by)
    library:pydantic (depends_on)
    resource:order-cache (depends_on)
    resource:order-stream (depends_on)
  
  referenced by:
    behavior:order-cancelled (at)
    behavior:order-placed [superseded] (at)
    component:cancellation (parent)
    interface:order-events (declared_by)
    data:order (owner_component)
    decision:event-log (applies_to)
    milestone:m1 (scope)
  
  '''
# ---
